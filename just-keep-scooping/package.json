{
  "name": "just-keep-scooping",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A tiny incremental about garbage, capitalism, and a raccoon in a tiny tie.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
